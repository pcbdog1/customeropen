"""Optional local seed records for the company domain pool.

The public distribution contains no real company records. Operators can add
their own permitted records through the documented import flow.
"""

LOCAL_CURATED_HARDWARE_SEEDS: list[tuple[str, str, str, str, str]] = []
